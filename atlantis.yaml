version: 3
automerge: false
projects:
  - name: ec2
    dir: ./terraform
    workflow: terraform
    autoplan:
      when_modified: ["*.tf", "*.tfvars"]
      enabled: true
workflows:
  terraform:
    plan:
      steps:
        - init
        - plan
