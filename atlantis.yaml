version: 3
automerge: false
projects:
  - name: ec2
    dir: ./terraform
    workflow: default
    autoplan:
      when_modified: ["*.tf", "*.tfvars"]
      enabled: true
workflows:
  default:
    plan:
      steps:
        - init
        - plan
